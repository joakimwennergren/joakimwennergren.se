import { Box, Container, Typography } from "@mui/material";


function Header() {
    return (
        <Box className="header" sx={{ height: "40vh", backgroundColor: "#fff", justifyContent: "center", alignItems: "center", display: "flex", flexDirection: "column" }}>
            <Container sx={{ display: "flex", justifyContent: "row", alignItems: "center" }}>

            </Container>
        </Box>
    );
}

export default Header;
